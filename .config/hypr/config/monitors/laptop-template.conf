# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃                   Laptop Monitor Template                  ┃
# ┃          Copy this file and rename to hostname.conf       ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

# To use this template:
# 1. Copy this file to {hostname}.conf (e.g., "laptop1.conf")
# 2. Modify the monitor configuration below for your specific laptop
# 3. Update the main monitor.conf to source your hostname's config

# Basic laptop configuration - modify as needed
# Internal laptop display
monitor = eDP-1, preferred, 0x0, 1

# External monitor (when connected) - uncomment and modify as needed
# monitor = HDMI-A-1, preferred, auto, 1

# Disable internal display when external connected - uncomment if desired
# monitor = eDP-1, disable